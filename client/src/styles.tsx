import { createStyles } from '@material-ui/core'

const styles = createStyles({
  h1: {
    marginTop: '0.5em',
    fontWeight: 400,
    fontSize: '3.5em'
  },
  light: {
    color: 'white'
  },

  text: {
    width: 400
  }

})

export default styles
